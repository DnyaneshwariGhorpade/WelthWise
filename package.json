{
  "name": "wealthwise",
  "version": "0.1.0",
  "private": true,
  "description": "WealthWise — AI-Powered Personal Wealth Planner (root workspace)",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "migrate": "npm run migrate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}